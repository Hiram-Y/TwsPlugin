<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wxsdklibrary"
    android:sharedUserId="com.example.pluginhost"
    android:versionCode="1"
    android:versionName="1.0.1" >

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light" >

        <!-- uses-library作用是用来通知插件框架此插件依赖哪些其他插件 -->
        <uses-library
            android:name="com.example.pluginbase"
            android:required="true" />

        <activity android:name="com.example.wxsdklibrary.TestSendToWXActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.pluginmain.wxapi.WXEntryActivity" />
    </application>

</manifest>